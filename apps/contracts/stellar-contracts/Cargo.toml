[workspace]
resolver = "2"
members = ["trex-token", "neko-distributor"]

[workspace.dependencies]
soroban-sdk = { version = "22.0.0", features = ["alloc"] }

[profile.release]
opt-level = "z"
overflow-checks = true
debug = 0
strip = "symbols"
debug-assertions = false
panic = "abort"
codegen-units = 1
lto = true

[profile.release-with-logs]
inherits = "release"
debug-assertions = true
